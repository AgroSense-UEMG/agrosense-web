# AgroSense - Plataforma Web 💻

Bem-vindo ao repositório oficial do **Squad Plataforma** do projeto AgroSense.
Aqui ficará todo o código da interface e Dashboard.

## 🚀 Tecnologias
* **React** + **Vite** + **TypeScript**
* **Tailwind CSS** (Estilização)
* **shadcn/ui** (Componentes)

## ⚠️ Regra de Ouro (LEIA ANTES DE CODAR)
A branch `main` está **bloqueada** para commits diretos.

**Como trabalhar:**
1.  Atualize sua `main`: `git pull origin main`
2.  Crie uma branch para sua tarefa: `git checkout -b feature/login-screen`
3.  Codifique e faça commits.
4.  Abra um **Pull Request (PR)** para a `main` quando terminar.

## 🛠️ Como rodar o projeto
1.  Instale as dependências: `npm install`
2.  Rode o servidor: `npm run dev`

